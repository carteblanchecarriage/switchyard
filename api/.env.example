# Keebshelf API Environment Configuration

# Server Configuration
PORT=3003
NODE_ENV=production

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://keebshelf.com,https://www.keebshelf.com,https://carteblanchecarriage.github.io

# Rate Limiting Configuration
# Window size in milliseconds (60000 = 1 minute)
RATE_LIMIT_WINDOW=60000
# Maximum requests per window per IP
# Development: 1000, Production: 100
RATE_LIMIT_MAX=100

# Data Configuration
# Path to data file (relative to api/ directory) or absolute path
DATA_FILE=../data/keyboard-data.json
# How often to reload data from disk (milliseconds)
# Default: 300000 (5 minutes)
DATA_RELOAD_INTERVAL=300000

# Request Timeout Configuration
# Maximum time for a request to complete (milliseconds)
REQUEST_TIMEOUT=30000

# Security Configuration
# Set to true when running behind a reverse proxy (nginx, cloudflare)
TRUST_PROXY=true

# Logging Configuration
# Options: error, warn, info, debug
LOG_LEVEL=info

# Optional: Analytics/Monitoring
# PLAUSIBLE_DOMAIN=keebshelf.com
# PLAUSIBLE_API_KEY=your_api_key_here

# Optional: Error Tracking
# SENTRY_DSN=https://public@sentry.example.com/1
